#include <iostream>

int main(){
    // If statements give you the options to give conditions, if its true
    // if it doesn't apply, it doesn't do it

    int year = 2024;
    
    if(year == 2024){
        std::cout << "It's correct, the year given is 2024";
    };

    return 0;
}